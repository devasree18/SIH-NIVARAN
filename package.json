{
  "name": "sih-nivaran",
  "version": "1.0.0",
  "description": "Smart Agricultural Procurement Management Platform - SIH 2026 Problem Statement 26032",
  "private": true,
  "scripts": {
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "build:server": "npm --prefix server install && npm --prefix server run prisma:generate && npm --prefix server run prisma:push && npm --prefix server run prisma:seed && npm --prefix server run build",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "build": "npm run build:server && npm run build:client",
    "start": "npm --prefix server start",
    "test": "npm --prefix server run test",
    "seed": "npm --prefix server run prisma:seed"
  }
}
